class ContactClass {
  id: number
  name: string
  tel: string
  email: string

  constructor(id: number, name: string, tel: string, email: string) {
    this.id = id
    this.name = name
    this.tel = tel
    this.email = email
  }
}

export default ContactClass
